# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
.eggs/
pip-wheel-metadata/

# pytest / coverage / type / lint cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# venv
.venv/
venv/
env/

# IDE / editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log

# Jupyter (workspace は JupyterLab 方向、checkpoints は無視)
.ipynb_checkpoints/

# Generated artifacts (regenerate via examples/*.py)
spring_mass_damper.png
pid_bode.png

# Local model storage for `pyflw-server --model-dir ./models` (= ユーザーデータ、
# repo に含めない)
models/

# Sphinx build output (regenerate via `sphinx-build -b html docs docs/_build`)
docs/_build/
docs/_autosummary/

# Frontend (ADR-0012)
node_modules/
pyflw/web/frontend/dist/
pyflw/web/frontend/.vite/
pyflw/web/frontend/tsconfig.tsbuildinfo
# StaticFiles 配信先: ローカル build 時のみ生成、release 時は別管理 (ADR-0012 §(8))
pyflw/server/static/

# Frontend logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright (E2E テスト出力)。`playwright.config.ts` の `cwd` 解決方法によっては
# repo root にも書き込まれるので両方無視する。
test-results/
playwright-report/
pyflw/web/frontend/test-results/
pyflw/web/frontend/playwright-report/
pyflw/web/frontend/playwright/.cache/

# Claude Code config / docs — local-only, not tracked (public 化しても非公開)
.claude/
